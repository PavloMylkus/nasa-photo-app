import React from "react"

const SolSectoion = ({ onChange }) => {
	return (
		<>
			<input type='number' placeholder="Number of Sol 1-1000" onChange={onChange} />
		</>
	)
};

export default SolSectoion;
